enum InterviewConfigType { technical, rrhh, mixed }

enum InterviewMode { simulated, realtime }

enum JobRole {
  frontendDeveloper,
  backendDeveloper,
  mobileDeveloper,
  uiUxDesigner,
  dataAnalyst,
  dataScientist,
  qaTester,
  devOps,
  productManager,
}
